# Explore simulated Power and Level of the t-test with this Shiny app

```
rm(list=ls()) )
require(shiny)
runGitHub("tPowerShiny,"DataScienceUWL")
```

If you can make help make the app more robust, then please share!